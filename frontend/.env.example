# Copy this file to `.env.local` (gitignored by Vite) and tweak as needed.

# URL of the NoorConnect Django backend, INCLUDING the trailing `/api/`.
# Local dev: http://localhost:8000/api/
# Production: https://api.noorconnect.pk/api/
VITE_API_BASE_URL=http://localhost:8000/api/
